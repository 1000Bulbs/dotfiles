# Usage: change_extension OLD NEW
# e.g., change_extension yml yaml
change_extension() {
  local old=$1 new=$2 f

  if [[ -z $old || -z $new ]]; then
    print -u2 "Usage: change_extension OLD_EXT NEW_EXT"
    return 1
  fi

  for f in **/*.$old(N.); do
    mv -- "$f" "${f%.$old}.$new"
  done
}
